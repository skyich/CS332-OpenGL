#version 330 core
in vec2 vert_texcoord;
in vec4 vert_color;
uniform bool use_texture;
uniform sampler2D texture;
out vec4 color;
void main() {
if(use_texture)
	color = vert_color * texture(texture, vert_texcoord);
else
	color = vert_color;
}